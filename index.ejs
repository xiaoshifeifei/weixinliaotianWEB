<!DOCTYPE html>
<html lang="zh-cmn-Hans">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0, minimum-scale=1.0, viewport-fit=cover">
    <link rel="icon" href="favicon.ico">
    <title>new im</title>
    <link rel="stylesheet" href="https://at.alicdn.com/t/font_1425251_3v0kq1by4iq.css">
    <link rel="stylesheet" href="https://cdn.bootcss.com/animate.css/3.7.2/animate.css">
    <script type="text/javascript" src="./static/forge.js"></script>
    <script type="text/javascript" src="./static/protobuf.js"></script>
    <script type="text/javascript" src="./static/jsencrypt.min.js"></script>

    <% if (htmlWebpackPlugin.options.nodeModules) { %>
        <script>
          require('module').globalPaths.push('<%= htmlWebpackPlugin.options.nodeModules.replace(/\\/g, '\\\\') %>')
        </script>
    <% } %>

    <style>
        .htmlClass {
            font-family: "Helvetica Neue", Helvetica, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "微软雅黑", Arial, sans-serif;
            /*padding:5px;*/
            box-sizing: border-box;
            height: 100%;
            width: 100%;
        }

        .bodyClass {
            /*height: calc(100% - 6px);*/
            /*width: calc(100% - 6px);*/
            /*padding: 2px;*/
            height: calc(100%);
            width: calc(100%);
            padding: 0px;
            margin: 0px;
            -webkit-user-select: none;
            border-radius: 3px;
            overflow: hidden;
            position: relative;
        }
        #app{
            width: 100%;
            height: 100%;
            /*width: calc(100% - 10px);*/
            /*height: calc(100% - 10px);*/
            /*left: 5px;*/
            /*top: 5px;*/
            position: absolute;
            border-radius: 4px;
        }
        .shadow{
            box-shadow: 0 0 3px rgba(0, 0, 0, 1);
        }


        h1, h2, h3, h4, h5, h6, p {
            margin: 0px;
            padding: 0px;
        }

        ::-webkit-scrollbar {
            width: 2px!important;
            height: 4px!important;
        }

        ::-webkit-scrollbar-track {
            background-color: #bee1eb;
        }

        ::-webkit-scrollbar-thumb {
            background-color: #00aff0;
        }

        ::-webkit-scrollbar-thumb:hover {
            background-color: #9c3;
            width: 2px;
            height: 4px;
        }

        ::-webkit-scrollbar-thumb:active {
            background-color: #00aff0
        }
    </style>

</head>

<body class="bodyClass" ondragstart="return false" ondrop="return false" ondragover="return false">
<div id="app">
    <div class="first-loading-wrp">
        <div class="loading-wrp">
            <span class="dot dot-spin"><i></i><i></i><i></i><i></i></span>
        </div>
    </div>
</div>
<!-- Placed js at the end of the document so the pages load faster -->
</body>

<script type="text/javascript">
  if (process.env.NODE_ENV !== 'development') window.__static = require('path').join(__dirname, '/static').replace(/\\/g, '\\\\')

  //
  // function toIt (parent, id) {
  //   let iDocument = document.getElementsByTagName('iframe')[0].contentWindow.document
  //   var timer = null
  //   var element = iDocument.getElementById(id).offsetTop
  //   iDocument.querySelector(parent).scrollTop = element - 50
  // }
  //
  // const electron = require('electron')
  // const win = electron.remote.getCurrentWindow()

  // win.on('maximize', function () {
  //     // let html = document.getElementsByTagName('html')[0]
  //     // html.style.padding = '0px'
  //     win.setMovable(false)
  // })
  //
  // win.on('unmaximize', function () {
  //     // let html = document.getElementsByTagName('html')[0]
  //     // html.style.padding = '5px'
  //     win.setMovable(true)
  // })
</script>
</html>
